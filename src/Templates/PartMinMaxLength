{% if minLength > 0 and maxLength > 0 %} length : {{ minLength }} <= x <= {{ maxLength }} chars{% elseif minLength > 0 %} length : >= {{ minLength }} chars{% elseif maxItems > 0 %} length : <= {{ maxLength }} chars{% endif %}