{% if minLength > 0 and maxLength > 0 %}between {{ minLength }} and {{ maxLength }} chars (included){% elseif minLength > 0 %}at least {{ minLength }} chars{% elseif maxItems > 0 %}no more than {{ maxLength }} chars{% endif %}