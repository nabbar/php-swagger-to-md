{% if minItems > 0 and maxItems > 0 %}{{ minItems }} <= Number of items <= {{ maxItems }}{% elseif minItems > 0 %}Number of items >= {{ minItems }}{% elseif maxItems > 0 %}Number of items <= {{ maxItems }}{% endif %}